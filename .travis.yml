sudo: required
language: node_js
node_js:
  - "12"
cache:
  directories:
    - "node_modules"

addons:
  sonarcloud:
    organization: "autowp-github"
    token:
      secure: $SONARCLOUD_TOKEN

install: npm install

script:
  - sonar-scanner -Dsonar.login=$SONARCLOUD_TOKEN || true
  - ./node_modules/.bin/ng build --prod --no-progress --base-href=/ --extra-webpack-config webpack.extra.js || travis_terminate 1;
  - docker build . -t autowp/autowp-frontend:oauth || travis_terminate 1;

after_success:
  - if [ "$TRAVIS_BRANCH" == "feature/oauth" ]; then
    docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push autowp/autowp-frontend:oauth;
    fi
